// import './slick/slick';

$(document).ready(function(){
  $('.fifth__slider').slick({
    // slidesToShow: 2,
    // dots: true,
    responsive: true,
    dots: true,
    infinite: true,
    speed: 300,
    slidesToShow: 1,
    adaptiveHeight: true,
    // arrows: true,
  })
});